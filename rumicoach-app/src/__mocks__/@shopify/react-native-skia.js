const React = require('react')
const { View } = require('react-native')

const StubView = React.forwardRef((props, ref) => React.createElement(View, { ...props, ref }))

const Noop = () => null
const NoopValue = (v) => ({ current: v ?? 0, value: v ?? 0 })

module.exports = {
  Canvas: StubView,
  Path: StubView,
  Circle: StubView,
  Text: StubView,
  Group: StubView,
  Rect: StubView,
  RRect: StubView,
  Line: StubView,
  Oval: StubView,
  Points: StubView,
  Patch: StubView,
  Vertices: StubView,
  Image: StubView,
  ImageSVG: StubView,
  Picture: StubView,
  Glyphs: StubView,
  TextBlob: StubView,
  TextPath: StubView,
  DiffRect: StubView,
  Atlas: StubView,
  Box: StubView,
  BoxShadow: StubView,
  RadialGradient: StubView,
  LinearGradient: StubView,
  SweepGradient: StubView,
  TwoPointConicalGradient: StubView,
  Color: StubView,
  Shader: StubView,
  Turbulence: StubView,
  FractalNoise: StubView,
  DashPathEffect: StubView,
  CornerPathEffect: StubView,
  DiscretePathEffect: StubView,
  Line2DPathEffect: StubView,
  Path1DPathEffect: StubView,
  Path2DPathEffect: StubView,
  SumPathEffect: StubView,
  Blend: StubView,
  ImageFilter: StubView,
  Blur: StubView,
  Shadow: StubView,
  BlurMask: StubView,
  Skia: {
    Path: { Make: () => ({ moveTo: Noop, lineTo: Noop, quadTo: Noop, addArc: Noop, close: Noop, toSVGString: () => '' }) },
    Paint: () => ({ setColor: Noop, setAlphaf: Noop, setShader: Noop, copy: () => ({}), dispose: Noop }),
    XYWHRect: () => ({}),
    Color: () => [],
    Font: () => ({ measureText: () => ({ width: 0, height: 0 }), getMetrics: () => ({ ascent: 0, descent: 0, leading: 0 }) }),
  },
  useFont: () => null,
  useCanvasRef: NoopValue,
  useValue: NoopValue,
  useComputedValue: NoopValue,
  useTiming: NoopValue,
  useLoop: NoopValue,
  useSpring: NoopValue,
  useClockValue: NoopValue,
  useValueEffect: Noop,
  useImage: () => null,
  vec: (x, y) => ({ x: x || 0, y: y || 0 }),
  rect: (x, y, w, h) => ({ x: x || 0, y: y || 0, width: w || 0, height: h || 0 }),
  rrect: (x, y, w, h, r) => ({ x: x || 0, y: y || 0, width: w || 0, height: h || 0, rx: r || 0, ry: r || 0 }),
}
